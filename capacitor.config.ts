import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'geolocation-example',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
